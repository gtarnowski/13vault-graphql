schema {
  query: Query
  mutation: Mutation
}

type News {
  id: ID
  title: String
  content: String
  date: String
}

type NewsAll {
  news: [News]
}

type Query {
  news: News
  newsAll: [News]
}

type Mutation {
  newsCreate(

    title: String!
  ): News
}
